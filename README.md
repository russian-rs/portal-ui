### Внесение изменений

Все изменения должны быть оформлены через Pull Request и пройти код ревью. Внесение изменений напрямую в main ветку
категорически запрещено, поскольку настроен автоматический деплой на стенд при любых изменениях в главной ветке.

Именование веток: обязательно префикс **feature/** или **bugfix/**, затем номер задачи и краткое описание, например:

- feature/23-notes-in-application
- bugfix/17-pagination

Название PR: обязательно префикс **feature/** или **bugfix/**, затем номер задачи и краткое описание на русском,
например:

- feature/23 Заметки в заявках
- bugfix/17 Доработка пагинации

### Запуск

1. Необходимо выпустить токен для доступа к npm пакетам API. Для этого нужно перейти
   в [настройки GitHub](https://github.com/settings/tokens) и создать classic токен со скоупом read:packages. В системе
   необходимо создать переменную среды **GITHUB_RUSSIAN_RS_NPM_TOKEN**.

2. Запустить приложение:

```
npm install
npm run build
npm run dev
```

По умолчанию в качестве backend используется тестовый стенд: [portal-test](https://portal-test.russian.rs), для входа у
вас должна быть заведена учетная запись на [тестовом авторизационном сервере](https://id-test.russian.rs), если такой
нет, то обратитесь к коллегам за помощью.

Поменять адрес бэкенда можно в файле vite.config.mts (переменная apiTarget). 


